CREATE TABLE compensation_type
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(100)
);
CREATE TABLE compensation_type_id_seq
(
);
CREATE TABLE competition
(
    id SERIAL PRIMARY KEY NOT NULL,
    formative_orgunit_id INT,
    competition_type_id INT,
    program_form_id INT,
    program_subject_id INT,
    level_type_id INT,
    program_set_ministerial_plan INT,
    program_set_target_adm_plan INT,
    program_set_exclusive_plan INT,
    program_set_contract_plan INT,
    compensation_type_id INT
);
CREATE TABLE competition_id_seq
(
);
CREATE TABLE competition_type
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(100)
);
CREATE TABLE competition_type_id_seq
(
);
CREATE TABLE discipline
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(20)
);
CREATE TABLE discipline_id_seq
(
);
CREATE TABLE edu_document_kind
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(100)
);
CREATE TABLE edu_document_kind_id_seq
(
);
CREATE TABLE entrant_competition
(
    entrantid INT,
    competition_id INT,
    priority INT,
    accepted INT
);
CREATE TABLE entrant_data
(
    id SERIAL PRIMARY KEY NOT NULL,
    first_name VARCHAR(20),
    last_name VARCHAR(20),
    middle_name VARCHAR(20),
    birth_date DATE,
    citizenship VARCHAR(20),
    edu_document_number INT,
    edu_document_average_mark INT,
    edu_document_kind_id INT
);
CREATE TABLE entrant_data_id_seq
(
);
CREATE TABLE exam_entrant
(
    entrant_id INT,
    discipline_id INT,
    mark INT
);
CREATE TABLE exam_entrant_id_seq
(
);
CREATE TABLE formative_orgunit
(
    id SERIAL PRIMARY KEY NOT NULL,
    full_title VARCHAR(100),
    short_title VARCHAR(20)
);
CREATE TABLE formative_orgunit_id_seq
(
);
CREATE TABLE level_type
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(100)
);
CREATE TABLE level_type_id_seq
(
);
CREATE TABLE program_form
(
    id SERIAL PRIMARY KEY NOT NULL,
    title VARCHAR(100)
);
CREATE TABLE program_form_id_seq
(
);
CREATE TABLE program_subject
(
    id SERIAL PRIMARY KEY NOT NULL,
    code VARCHAR(10),
    title VARCHAR(100)
);
CREATE TABLE program_subject_id_seq
(
);
CREATE TABLE users
(
    id SERIAL PRIMARY KEY NOT NULL,
    name VARCHAR(20),
    password VARCHAR(100)
);
CREATE TABLE users_id_seq
(
);
ALTER TABLE competition ADD FOREIGN KEY (compensation_type_id) REFERENCES compensation_type (id);
ALTER TABLE competition ADD FOREIGN KEY (competition_type_id) REFERENCES competition_type (id);
ALTER TABLE competition ADD FOREIGN KEY (formative_orgunit_id) REFERENCES formative_orgunit (id);
ALTER TABLE competition ADD FOREIGN KEY (level_type_id) REFERENCES level_type (id);
ALTER TABLE competition ADD FOREIGN KEY (program_form_id) REFERENCES program_form (id);
ALTER TABLE competition ADD FOREIGN KEY (program_subject_id) REFERENCES program_subject (id);
ALTER TABLE entrant_competition ADD FOREIGN KEY (competition_id) REFERENCES competition (id);
ALTER TABLE entrant_competition ADD FOREIGN KEY (entrantid) REFERENCES entrant_data (id);
ALTER TABLE entrant_data ADD FOREIGN KEY (edu_document_kind_id) REFERENCES edu_document_kind (id);
ALTER TABLE exam_entrant ADD FOREIGN KEY (discipline_id) REFERENCES discipline (id);
ALTER TABLE exam_entrant ADD FOREIGN KEY (entrant_id) REFERENCES entrant_data (id);
